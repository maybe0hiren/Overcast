1. **makeUID(filePath: str, fileName: str)**: generate a 265bit consistent id with the given strings
2. **makeLink(UID: str)**: make a shareable link for a file [[(dot)env]]
3. **decodeLink(link: str)**: decode the UID of the file from the link[[(dot)env]]